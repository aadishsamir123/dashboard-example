import React from 'react';
import './SplashScreen.css'; // Import the CSS for spinner

const SplashScreen = () => {
  return (
    <div className="splash-screen">
      <div className="spinner"></div>
    </div>
  );
};

export default SplashScreen;
